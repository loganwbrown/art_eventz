<h1>Editing event</h1>
<p>DASHBOARD/EDIT</p>

<%= render 'form' %>

<h5>
<%= link_to 'Back', dashboard_events_path %> | 
<%= link_to 'Show Event', @event %> |
<%= link_to 'Delete Event', dashboard_event_path(@event.id), method: :delete, data: { confirm: 'Are you sure?' } %></h5>